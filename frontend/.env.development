# In development, the Vite proxy handles /api requests to PocketBase.
# The VITE_PB_PROXY env var in docker-compose sets the proxy target.
# Set VITE_PB_URL only for production deployments (no proxy available).
